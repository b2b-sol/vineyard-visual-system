# Vineyard Visual System — Codex Seed Packet

This ZIP is a self-contained starting packet for a long-running Codex Goal. Extract it into a local working folder that Codex can access, authenticate GitHub as the `b2b-sol` organization owner, and send the contents of `PROMPT_TO_START.txt` or attach the ZIP and direct Codex to `START_HERE.md`.

Codex must stop after creating the public repository and inviting `tiklebot`. The repository remains empty until the bot-authenticated phase begins.

The packet contains the workflow report, plans, assumptions, source hierarchy, autonomous decision rules, GitHub/auth protocol, current Codex model/subagent strategy, Figma-ready requirements, schemas, templates, custom agents, bootstrap scripts, task registry, state files, and Definition of Done.
